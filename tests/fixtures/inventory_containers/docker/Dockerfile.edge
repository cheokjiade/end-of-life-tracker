FROM scratch AS empty
FROM python AS untagged
FROM python:latest AS newest
FROM golang:1.23@sha256:0123456789abcdef AS pinned
ARG BASE_IMAGE
FROM ${BASE_IMAGE} AS chosen
FROM golang:1.23.0-alpine AS build
FROM build AS reuse
FROM \
  alpine:3.20 AS final
